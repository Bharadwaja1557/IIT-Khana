# Personal working notes — never commit
.notes/
CLAUDE.md

# Secrets
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Local data
*.db
# Derived vector index — rebuildable via `python -m scripts.build_index`
db/chunks.*
*.sqlite
*.sqlite3
data/raw/
data/cache/
